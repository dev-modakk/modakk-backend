{
  "compilerOptions": {
    "target": "ES2020", // modern JS output
    "module": "CommonJS", // works best for Node + ts-node-dev
    "moduleResolution": "Node", // how modules are resolved
    "outDir": "dist", // compiled output folder
    "rootDir": "src", // your source folder
    /* Strictness */
    "strict": true, // enable all strict checks
    "esModuleInterop": true, // allow import express from "express"
    "skipLibCheck": true, // skip type checks in node_modules
    /* Types */
    "types": [
      "node"
    ], // pull in Node.js types
    "lib": [
      "ES2020"
    ], // no DOM types, only ES2020 APIs
    /* Source Maps (optional for debugging) */
    "sourceMap": true
  },
  "baseUrl": "src", // <-- root for non-relative imports
  "paths": {
    "@/*": [
      "*"
    ] // <-- alias "@" to src/
  },
  "include": [
    "src"
  ], // compile everything in src/
  "exclude": [
    "node_modules",
    "dist"
  ] // ignore build + deps
}